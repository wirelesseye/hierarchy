[package]
name = "hierarchy"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
proc_macros = { package = "hierarchy_proc_macros", path = "./proc_macros"}